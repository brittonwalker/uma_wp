import { __ } from '@wordpress/i18n';
import { useBlockProps } from '@wordpress/block-editor';

import Controls from "./controls";
import './editor.scss';

export default function Edit(props) {
	return (
		<>
			<Controls {...props} />
			<div { ...useBlockProps() }>
				{ __( 'UMA Blocks – hello from the editor!', 'uma-blocks' ) }
			</div>
		</>
	);
}
